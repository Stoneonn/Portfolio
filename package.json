{
  "name": "otaskaya-me",
  "version": "4.0.0",
  "private": true,
  "description": "otaskaya.me — the 'One Day' site (site/) and the MDW guide sub-app (designweek/). No dependencies live here; each app owns its own.",
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "dev:designweek": "cd designweek && npm install && npm run dev",
    "build:designweek": "cd designweek && npm install && npm run build && rm -rf ../site/public/designweek && mkdir -p ../site/public/designweek && cp -r dist/. ../site/public/designweek/"
  },
  "engines": {
    "node": ">=20"
  }
}
